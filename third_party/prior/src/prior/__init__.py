"""Prior — turn primary literature into a queryable atlas of claims.

Three agents:
  Reader        paper            -> structured claims
  Cartographer  bag of claims    -> atlas graph
  Navigator     question + atlas -> grounded answer (forward & backward)
"""

__version__ = "0.1.0"
